__pycache__
.claude
.cursor
.pytest_cache
.ruff_cache
.serena
.venv
agent_workspace
.env
/logs/
server.log
/server.*.log
debug-*.log
.coverage
llama_cache
.smoke-results
Microsoft/
/test-results/
.vscode

# Agentic directives live in the private 'directives' submodule (fcc-directives).
# Root CLAUDE.md/AGENTS.md are tracked @import stubs — never put real directive
# content in them; `git add -f` on a real copy is considered a leak.
